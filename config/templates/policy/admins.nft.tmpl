# Individual administrator Tailscale IPv4 addresses only.
# Empty by default: management access is DENIED until populated.
# Example syntax inside the set: elements = { 100.101.102.103 }
set admins4 {
    type ipv4_addr
}
